apiVersion: v1
kind: Service
metadata:
  name: maintenance-service
  namespace: lams-prod
spec:
  selector: { app: maintenance-service }
  ports:
  - port: 8080
    targetPort: 8080
  type: ClusterIP
